import { define } from "build/define"

const views = define(() => {

    const lib = {
        components,
        includes,
        layouts,
        styles
    }

    return lib
})

export default views 
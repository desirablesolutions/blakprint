import { define } from "build/index"

const views = define(() => {

    const lib = {
        components,
        includes,
        layouts,
        styles
    }

    return lib
})

export default views 
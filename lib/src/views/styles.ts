import { define } from "build/index"


const styles = define(() => {
    
    const lib = {
        methods: {
            defineStylizer: {

            }
        }
    }

    return lib
})

export default styles